services:
  # MCP Server
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.example
    environment:
      DEBUG: ${DEBUG:-false}
    ports:
      - "${WEB_PORT:-8000}:8000"
    volumes:
      - ./app:/app/app
